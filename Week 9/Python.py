# Arithmetic operators
# + addition
# - subtraction
# * multiplication
# / division
# % modulus
# ** exponent
# // floor division

# Comparison operators
# == equal to
# != not equal to
# > greater than
# < less than
# >= greater than or equal to
# <= less than or equal to

# Logical operators
# and
# or
# not

# Membership operators
# in
# not in

# Identity operators
# is
# is not

# Bitwise operators
# &
# |
# ^
# ~
# <<
# >>

# Python has the following built-in functions
# abs() - returns the absolute value of a number
# all() - returns True if all items in an iterable object are true
# any() - returns True if any item in an iterable object is true
# ascii() - returns a readable version of an object, escape characters are replaced with \x, \u or \U
# bin() - returns the binary version of a number
# bool() - returns the boolean value of the specified object
# bytearray() - returns an array of bytes
# bytes() - returns a bytes object
# callable() - returns True if the specified object is callable, otherwise False
# chr() - returns a character from the specified Unicode code
# classmethod() - converts a method into a class method
# compile() - returns the specified source as an object, ready to be executed
# complex() - returns a complex number
# delattr() - deletes the specified attribute (property or method) from the specified object
# dict() - returns a dictionary (Array)
# dir() - returns a list of the specified object's properties and methods
# divmod() - returns the quotient and the remainder when argument1 is divided by argument2
# enumerate() - takes a collection (e.g. a tuple) and returns it as an enumerate object
# eval() - evaluates and executes an expression
# exec() - executes the specified code (or object)
# filter() - use a filter function to exclude items in an iterable object
# float() - returns a floating point number
# format() - formats a specified value
# frozenset() - returns a frozenset object
# getattr() - returns the value of the specified attribute (property or method)
# globals() - returns the current global symbol table as a dictionary
# hasattr() - returns True if the specified object has the specified attribute (property/method)
# hash() - returns the hash value of a specified object
# help() - executes the built-in help system
# hex() - converts a number into a hexadecimal value
# id() - returns the id of an object
# input() - allows user input
# int() - returns an integer number
# isinstance() - returns True if a specified object is an instance of a specified object
# issubclass() - returns True if a specified class is a subclass of a specified object
# iter() - returns an iterator object
# len() - returns the length of an object
# list() - returns a list
# locals() - returns an updated dictionary of the current local symbol table
# map() - returns the specified iterator with the specified function applied to each item
# max() - returns the largest item in an iterable
# memoryview() - returns a memory view object
# min() - returns the smallest item in an iterable
# next() - returns the next item in an iterable
# object() - returns a new object
# oct() - converts a number into an octal
# open() - opens a file and returns a file object
# ord() - converts a character into a Unicode code
# pow() - returns the value of x to the power of y
# print() - prints to the standard output device
# property() - gets, sets, deletes a property
# range() - returns a sequence of numbers, starting from 0 and increments by 1 (by default)
# repr() - returns a readable version of an object
# reversed() - returns a reversed iterator
# round() - rounds a numbers
# set() - returns a new set object
# setattr() - sets an attribute (property/method) of an object
# slice() - returns a slice object
# sorted() - returns a sorted list
# @staticmethod() - converts a method into a static method
# str() - returns a string object
# sum() - sums the items of an iterator
# super() - returns an object that represents the parent class
# tuple() - returns a tuple
# type() - returns the type of an object
# vars() - returns the __dict__ property of an object
# zip() - returns an iterator, from two or more iterators

# Python has the following built-in modules
# datetime - provides classes for manipulating dates and times
# math - provides mathematical functions
# os - provides a portable way of using operating system dependent functionality
# random - provides tools for making random selections
# statistics - provides functions for calculating mathematical statistics of numeric data
# sys - provides access to some variables used or maintained by the interpreter and to functions that interact strongly with the interpreter
# time - provides various time-related functions
# urllib - provides a high-level interface for fetching data across the World Wide Web
# json - provides functions for working with JSON data
# re - provides regular expression matching operations
# sqlite3 - provides a lightweight disk-based database that doesn’t require a separate server process and allows accessing the database using a nonstandard variant of the SQL query language
# tkinter - provides a fast and easy way to create GUI applications
# turtle - provides turtle graphics primitives, in both object-oriented and procedure-oriented ways
# unittest - provides a framework for constructing test cases, and the ability to execute these tests and report the results
# xml - provides support for XML processing
# csv - provides classes for reading and writing tabular data in CSV format
# collections - provides specialized container datatypes
# itertools - provides functions creating iterators for efficient looping
# functools - provides higher-order functions and operations on callable objects
# operator - provides functions implementing the basic operators of Python
# copy - provides functions for shallow and deep copy operations
# pprint - provides a capability to “pretty-print” arbitrary Python data structures in a form which can be used as input to the interpreter
# pickle - implements binary protocols for serializing and de-serializing a Python object structure
# shelve - provides a persistent dictionary-like object
# sqlite3 - provides a lightweight disk-based database that doesn’t require a separate server process and allows accessing the database using a nonstandard variant of the SQL query language
# timeit - provides a simple way to time small bits of Python code
# logging - provides a flexible event logging system for applications and libraries
# threading - provides higher-level threading interfaces
# multiprocessing - provides a package that supports spawning processes using an API similar to the threading module
# subprocess - allows you to spawn new processes, connect to their input/output/error pipes, and obtain their return codes
# socket - provides access to the BSD socket interface
# select - provides access to the select() and poll() functions available in most operating systems, epoll() available on Linux 2.5+ and kqueue() available on most BSD
# asyncio - provides infrastructure for writing single-threaded concurrent code using coroutines
# concurrent - provides a high-level interface for asynchronously executing callables
# ssl - provides a TLS/SSL wrapper for socket objects
# email - provides a package for managing email messages
# json - provides an API for converting in-memory Python objects to a serialized representation known as JavaScript Object Notation (JSON)
# base64 - provides functions for encoding binary data to printable ASCII characters and decoding such encodings back to binary data
# html - provides functions for parsing HTML
# xml - provides functions for parsing XML
# tkinter - provides a fast and easy way to create GUI applications
# turtle - provides turtle graphics primitives, in both object-oriented and procedure-oriented ways
# unittest - provides a framework for constructing test cases, and the ability to execute these tests and report the results
# doctest - provides a tool for scanning a module and validating tests embedded in a program’s docstrings
# pdb - The Python Debugger for interactive interpreting and debugging
# profile - provides a C interface to the Python profiler
# cProfile - provides deterministic profiling of Python programs
# timeit - provides a simple way to time small bits of Python code

# Python has the following keywords
# False - boolean false
# None - null object
# True - boolean true
# and - logical and
# as - part of the with-as statement
# assert - assertion
# async - asynchronous function
# await - asynchronous function
# break - break out of a loop
# class - class definition
# continue - continue to the next iteration of a loop
# def - function definition
# del - delete a variable or attribute
# elif - else if condition
# else - else condition
# except - exception
# finally - exception
# for - for loop
# from - import specific attributes from a module
# global - declare a global variable
# if - if condition
# import - import a module
# in - part of the for loop
# is - is equal to
# lambda - anonymous function
# nonlocal - declare a non-local variable
# not - logical not
# or - logical or
# pass - do nothing
# raise - raise an exception
# return - return a value from a function
# try - try this block and if exception go to the except block
# while - while loop
# with - with a context manager
# yield - yield a value from a generator
